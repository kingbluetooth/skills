import {ListPlugin} from '@lexical/react/LexicalListPlugin';

export function EditorLists() {
  return <ListPlugin />;
}

// Register list nodes and test nesting, keyboard shortcuts,
// and copy/paste behavior.
